// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // User site (username.github.io), so no `base` path is needed.
  site: 'https://stephenmoy14.github.io',
});
